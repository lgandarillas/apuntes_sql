/***
hello-sql
By: @mouredev

11_sum.sql
***/


-- Suma todos los valores del 'atributo' de la 'tabla'
Select SUM(atributo) FROM tabla;
-- Suma todos los valores del campo edad de la tabla "users"
Select SUM(age) FROM users;


-- Suma todos los valores el 'atributo' de la 'tabla'
Select SUM(atributo) FROM tabla;
-- Suma todos los valores el campo edad de la tabla "users"
Select SUM(age) FROM users;
