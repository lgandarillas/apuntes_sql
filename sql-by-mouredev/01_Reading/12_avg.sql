/***
hello-sql
By: @mouredev

12_avg.sql
***/

-- Obitne la media de 'atributo' de la 'tabla'
Select AVG(atributo) FROM tabla;
-- Obitne la media de edad de la tabla "users"
Select AVG(age) FROM users;
